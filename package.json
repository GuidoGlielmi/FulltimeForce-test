{
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/server/dist/main"
  },
  "devDependencies": {
    "turbo": "^1.10.16"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "monorepo-globals": "*"
  }
}
